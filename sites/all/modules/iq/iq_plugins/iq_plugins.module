<?php
/**
 * @file
 * Custom plugins for IssueQ.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function iq_plugins_ctools_plugin_directory($owner, $plugin_type) {
  if (($owner == 'ctools') || ($owner == 'panels')) {
    return "plugins/$plugin_type";
  }
}